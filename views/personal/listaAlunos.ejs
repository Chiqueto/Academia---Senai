<!DOCTYPE html>
<html>
  <head>
    <meta lang="pt-br" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/styles/output.css" rel="stylesheet" />
    <title>Lista de Alunos</title>
  </head>
  <body>
    <h1>Lista de Alunos</h1>
  
    <!-- Verifica se há uma mensagem para exibir -->
    <% if (message) { %>
      <div style="color: red; font-weight: bold;">
        <p><%= message %></p>
      </div>
    <% } %>
  
    <!-- Exibe a lista de alunos -->
    <% if (alunos && alunos.length > 0) { %>
      <ul>
        <% alunos.forEach(aluno => { %>
          <li>
            <strong>Nome:</strong> <%= aluno.nome %><br>
            <strong>Email:</strong> <%= aluno.email %><br>
            <strong>Idade:</strong> <%= aluno.idade %>
          </li>
          <hr>
        <% }); %>
      </ul>
    <% } else { %>
      <!-- Mostra algo padrão quando não há alunos (opcional) -->
      <p>Não há alunos cadastrados no momento.</p>
    <% } %>
  
    <!-- Outras opções da página -->
    <a href="/personal/dashboard">Voltar ao Dashboard</a>
  </body>
</html>
