<!DOCTYPE html>
<html>
  <head>
    <meta lang="pt-br" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/output.css" />
    <title>Editar Perfil Academia</title>
  </head>

  <body class="flex flex-col min-h-screen">
    <header>
      <nav class="flex flex-grow bg-zinc-800 py-5 px-4">
        <div class="flex items-center gap-4">
          <a href="/academia/perfil/<%= academia.id %>"><img src="/icons/back_arrow.svg" /></a>
          <h1 class="font-jaro text-zinc-50 text-3xl">GymFit</h1>
        </div>
      </nav>
    </header>

    <main class="rounded-3xl mx-4 bg-zinc-50 text-black font-zain flex-grow">
      <section class="font-zain px-4">
        <h2 class="text-4xl text-center uppercase">Editar Dados</h2>
        <form action="/academia/atualizar/<%= academia.id %>" method="POST">
          <input type="hidden" name="_method" value="PUT" />
          <div class="mb-4">
            <label for="nome" class="block text-sm font-medium text-gray-700">Nome</label>
            <input type="text" name="nome" id="nome" value="<%= academia.nome %>" class="mt-1 block w-full p-2 border border-gray-300 rounded-md" required />
          </div>
          <div class="mb-4">
            <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
            <input type="email" name="email" id="email" value="<%= academia.email %>" class="mt-1 block w-full p-2 border border-gray-300 rounded-md" required />
          </div>
          <div class="mb-4">
            <label for="telefone" class="block text-sm font-medium text-gray-700">Telefone</label>
            <input type="text" name="telefone" id="telefone" value="<%= academia.telefone %>" class="mt-1 block w-full p-2 border border-gray-300 rounded-md" required />
          </div>
          <div class="mb-4">
            <label for="senha" class="block text-sm font-medium text-gray-700">Senha</label>
            <input type="password" name="senha" id="senha" class="mt-1 block w-full p-2 border border-gray-300 rounded-md" />
          </div>
          <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-md">Atualizar</button>
        </form>
      </section>
    </main>

    <footer class="text-zinc-200">
      <p>&copy; Luís Felipe, Edney Araujo, Marília</p>
    </footer>
  </body>
</html>