<!DOCTYPE html>
<html>
  <head>
    <meta lang="pt-br" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/output.css" />
    <script src="/scripts/academiaCadastro.js" defer></script>

    <title>Editar Perfil Academia</title>
  </head>

  <body class="flex flex-col min-h-screen">
    <header>
      <nav class="flex items-center bg-zinc-800 py-5 px-4">
        <div class="flex items-center gap-4">
          <a href="/academia/perfil/<%= academia.id %>"><img src="/icons/back_arrow.svg" /></a>
          <h1 class="font-jaro text-zinc-50 text-3xl">GymFit</h1>
        </div>
      </nav>
    </header>

    <main class="text-center my-10 text-white mx-4 flex-grow">
      <section class="font-zain bg-zinc-50 text-zinc-950 py-4 rounded-2xl">
        <h2 class="text-4xl">Editar Dados</h2>
        <form
          action="/academia/atualizar/<%= academia.id %>"
          method="POST"
          class="flex flex-col text-2xl gap-6 text-black max-w-screen-sm"
        >
          <div class="flex flex-col gap-2 px-6">
            <label class="text-start" for="nome">Nome</label>
            <input
              class="border-2 border-black rounded-full bg-pink-50 text-lg px-2 py-1 text-center"
              type="text"
              name="nome"
              value="<%= academia.nome %>"
              required
            />
          </div>

          <div class="flex flex-col gap-2 px-6">
            <label class="text-start" for="cidade">Cidade</label>
            <input
              class="border-2 border-black rounded-full bg-pink-50 text-lg px-2 py-1 text-center"
              type="text"
              name="cidade"
              value="<%= academia.cidade %>"
              required
            />
          </div>

          <div class="flex flex-col gap-2 px-6">
            <label class="text-start" for="bairro">Bairro</label>
            <input
              class="border-2 border-black rounded-full bg-pink-50 text-lg px-2 py-1 text-center"
              type="text"
              name="bairro"
              value="<%= academia.bairro %>"
              required
            />
          </div>

          <div class="flex flex-col gap-2 px-6">
            <label class="text-start" for="logradouro">Rua</label>
            <input
              class="border-2 border-black rounded-full bg-pink-50 text-lg px-2 py-1 text-center"
              type="text"
              name="logradouro"
              value="<%= academia.logradouro %>"
              required
            />
          </div>

          <div class="flex flex-col gap-2 px-6">
            <label class="text-start" for="telefone">Telefone</label>
            <input
              class="border-2 border-black rounded-full bg-pink-50 text-lg px-2 py-1 text-center"
              type="text"
              name="telefone"
              value="<%= academia.telefone %>"
              required
            />
          </div>

          <button
            type="submit"
            class="bg-neonGreen rounded-xl py-2 hover:bg-neonGreenHover transition-colors text-zinc-50 font-zain text-4xl mx-12"
          >
            Salvar
          </button>
        </form>
      </section>
    </main>

    <footer class="text-zinc-200 p-4 text-center">
      <p>&copy; Luís Felipe, Edney Araujo, Marília</p>
    </footer>
  </body>
</html>
